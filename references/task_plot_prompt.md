Use case: infographic-diagram
Asset type: TaskBeacon task flow diagram
Primary request: Create a clean, publication-ready task flow diagram as a timeline collection for the behavioral task described below.

Task: Tapping Synchronization Task
Construct: sensorimotor synchronization / auditory-motor timing
Rows/conditions:
- Practice 600 ms: one practice trial.
- Test 450 ms: scored tempo condition.
- Test 600 ms: scored tempo condition.
- Test 750 ms: scored tempo condition.

Timeline phases:
- Practice 600 ms: Ready (800 ms; press SPACE to begin block earlier) -> Sync tapping (10 beats x 600 ms = 6000 ms; press SPACE with metronome) -> Continuation tapping (30 beats x 600 ms = 18000 ms; press SPACE silently) -> Practice break (1200 ms; no response)
- Test 450 ms: Ready (800 ms; no response) -> Sync tapping (10 beats x 450 ms = 4500 ms; press SPACE with metronome) -> Continuation tapping (30 beats x 450 ms = 13500 ms; press SPACE silently) -> Test ITI (800 ms; no response; +)
- Test 600 ms: Ready (800 ms; no response) -> Sync tapping (10 beats x 600 ms = 6000 ms; press SPACE with metronome) -> Continuation tapping (30 beats x 600 ms = 18000 ms; press SPACE silently) -> Test ITI (800 ms; no response; +)
- Test 750 ms: Ready (800 ms; no response) -> Sync tapping (10 beats x 750 ms = 7500 ms; press SPACE with metronome) -> Continuation tapping (30 beats x 750 ms = 22500 ms; press SPACE silently) -> Test ITI (800 ms; no response; +)

Visual requirements:
- White background, landscape orientation, crisp dark text, restrained condition accent colors.
- One horizontal row per tempo condition.
- Each row contains 4 participant-screen snapshots connected by a subtle arrow.
- Each screen snapshot shows participant-visible screen content only.
- Use simple metronome tick marks and a SPACE key label; do not draw people.
- Use gray participant-screen boxes, thin black arrows, consistent row spacing, and subtle row separators.
- Place timing labels under each screen in compact text.
- Place tempo labels at the left of each row.
- Use short labels only; avoid paragraphs inside the image.
- Make all text legible at normal document preview size.
- Leave a clean blank header band across the top 18-20% of the image.

Accuracy constraints:
- Do not invent phases, stimuli, condition names, keys, feedback, rewards, or timings.
- Do not add people, lab equipment, decorative scenes, logos, or unrelated icons.
- Do not draw the task title, construct subtitle, any logo, watermark, brand mark, or `TaskBeacon` text inside the generated image.
- Draw only the timeline content below the blank header band.
- Preserve these exact terms where used: Practice 600 ms, Test 450 ms, Test 600 ms, Test 750 ms, SPACE, 10 beats, 30 beats, 800 ms, 1200 ms, 4500 ms, 6000 ms, 7500 ms, 13500 ms, 18000 ms, 22500 ms.

Style:
TaskBeacon scientific infographic style: clean vector-like raster image, organized spacing, gray screen boxes, restrained color accents, and a blank header-safe area.
